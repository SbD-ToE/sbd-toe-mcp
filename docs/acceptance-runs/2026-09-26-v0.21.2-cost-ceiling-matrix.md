# Matriz de 116 casos — tectos por contagem (0.21.1) vs regra de custo (0.21.2) — 2026-09-26

Decisão: `agentic/decisions/0004-tectos-por-custo-medido.md`. Reprodutível com
`node scripts/measure/cost-ceiling-matrix.mjs <dist-0.21.1> [out.json]` (o «antes» é o `dist/` de `f4b5564`,
construído num worktree à parte). Dados completos: `2026-09-26-v0.21.2-cost-ceiling-matrix.json`. Pino KG v1.12.0.

**Casos:** cada concern isolado em L2 e L3, os oito trios do vocabulário em L3, o overlay RGPD (auth+logging/L2) e a API
pública/L3 com activadores — 58 declarações, em `lista` e `standard`.

| resultado | casos |
|---|---:|
| iguais — prontos com as duas regras | 87 |
| iguais — bloqueados com as duas regras | 6 |
| prontos na 0.21.1 → `needs_decomposition` na 0.21.2 | 22 |
| bloqueados na 0.21.1 → prontos na 0.21.2 | 1 |
| prontos na 0.21.1 **acima do envelope** (a promessa por cumprir) | 22 |
| **violações da promessa na 0.21.2** | **0** |
| `full` byte-idêntico à 0.21.1 | 58 / 58 |

**As 22 que passam a decompor** são exactamente as 22 que a 0.21.1 servia prontas acima do envelope: `supply_chain`,
`deployment` e `agents` em L2 e L3, `release` em L3, três trios de L3 e o overlay RGPD — em `lista` e `standard`.
**A que desbloqueia:** `auth`+`logging`+`validation` em L3, `lista` — 53 requisitos baratos, 7 981 tk, que a 0.21.1 bloqueava só
por passar de 52.

**O que se provou em cada caso da 0.21.2:** uma resposta pronta cabe no envelope (ou é irredutível declarada — nenhum destes
casos o é); cada lote de uma decomposição, **executado** com a receita tal e qual, sai pronto (sem ciclo), recebe exactamente o
`measured_tk` que declarou, cabe no envelope, e a união dos lotes cobre a selecção do `full` (recall 1).

**Latência:** a chamada mais lenta da matriz levou 313 ms (uma decomposição, com as medições dos lotes); a matriz inteira, com os
lotes executados e o «antes», 32,9 s.
