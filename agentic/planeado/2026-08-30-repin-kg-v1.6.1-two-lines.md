# Planeado — re-pin formal KG `v1.6.1` nas duas linhas (0.10.3 · 0.20.0-beta.4)

**Date:** 2026-08-30 · **Persona:** Pontifex · **Dispatcher:** programme lead (mensagem 2026-08-30, após merge do #49)
**Gate:** Codex publica a release formal `v1.6.1` (camada de ligações requisito→controlo curada) + move `mcp-stable`.

## Linha estável → `0.10.3`
1. Verificar `refs/heads/mcp-stable` → `v1.6.1^{commit}`; `sync-bundle --from-release v1.6.1 --repo SbD-ToE/sbd-toe-knowledge-graph` (sha256 do asset vs sidecar; `run_manifest.release = stable/v1.6.1`); diff de `data/` vs v1.6.0 reportado.
2. Conteúdo: #49 (runner de aceitação, fix `query_entities`, limpeza `SnapshotCache`) já em `master` (`8ade07f`) + a camada de ligações curada que vier no bundle.
3. `npm run check` · `npm test` · **`npm run eval:acceptance`** (registo novo em `docs/acceptance-runs/`; comparar com 0.10.2 — TC-E-01/02 só mudam se o Codex publicar `associated_controls` como ids, ou se o critério mudar).
4. Bump 0.10.3 + CHANGELOG (mover *Unreleased* para 0.10.3; pin v1.6.1) + FREEZE-REGISTRY (linha 0.10.3, pins exactos) → PR → merge (lead/Orchestrator) → tag anotada `v0.10.3` no squash → `release.yml` → confirmar `npm view` (version/dist-tags/gitHead).

## Linha beta → `0.20.0-beta.4` (clone irmão `sbd-toe-mcp-poc_0.20.0`, branch `0.20-beta`)
1. **`git cherry-pick 8ade07f`** (o #49) antes do re-pin; resolver conflitos com o engine R&D se existirem; suite verde.
2. Re-pin v1.6.1 como acima (mesmo asset/sha256); `eval:acceptance` na beta também.
3. Bump `0.20.0-beta.4` + CHANGELOG da linha; tag prerelease (`release.yml` só publica `latest` para `vX.Y.Z` — confirmar o fluxo prerelease/`beta` dist-tag antes de tagar, ver memória `dual-line-0.20-beta-strategy`).

## Não fazer
Sem release enquanto `v1.6.1` não existir em `origin` do KG; sem mover `mcp-stable` (é do Codex); FREEZE-REGISTRY só no commit da release.
