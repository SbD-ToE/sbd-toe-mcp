# Release 0.21.0 — PREPARAÇÃO (Pontifex, 2026-09-25). Os actos são do lead.

**Estado do ramo `0.21`:** sete fases fechadas e aceites (§5, §1, §3, §2, decisão (a)+§6-a, §6, §7); `package.json` já em
**0.21.0** (bump preparado, sem tag). Pino **KG v1.12.0** inalterado. Bateria: vitest verde · `check` · smoke ·
`eval:acceptance` gate PASS · Eixo H 10/10 · invariante 3 vs oráculo 0.20.0 ✅.
Release notes: secção «0.21.0 — RASCUNHO» no `CHANGELOG.md` (tabela final + 20 diferenças de superfície).

## Actos do lead (por esta ordem — nenhum é do Pontifex)

1. **PR** `0.21` → `master`, **merge squash** (como sempre; título sugerido: `release(0.21.0): a forma da resposta — requisito fundido, três níveis, tectos 52/55, adjacência, notas por referência`).
2. Após merge: `git fetch && git checkout master && git reset --hard origin/master`.
3. **FREEZE-REGISTRY.md**: adicionar a linha de registo do estado `v0.21.0` **no mesmo commit** do tag (protocolo §5) —
   texto sugerido abaixo — e o commit `docs(release): FREEZE-REGISTRY v0.21.0`.
4. **Tag anotado** `v0.21.0` no commit da linha do registo: `git tag -a v0.21.0 -m "0.21.0 — a forma da resposta" && git push origin v0.21.0`.
   O `release.yml` publica no npm em `--tag next` (OIDC) e cria a GitHub Release.
5. Verificar `npm view @shiftleftpt/sbd-toe-mcp dist-tags` (`next: 0.21.0`); correr `npm run eval:acceptance` contra o pacote
   publicado se o runner o suportar; só depois **mover `latest`**: `npm dist-tag add @shiftleftpt/sbd-toe-mcp@0.21.0 latest` — acto do lead.
6. Espelho no hub + CHANGELOG: converter o «RASCUNHO» em «0.21.0 — YYYY-MM-DD» com o sha do tag (nunca inventado).

## Linha sugerida para o FREEZE-REGISTRY (preencher o sha REAL do tag)

| YYYY-MM-DD | v0.21.0 registered (linha 0.21 — a forma da resposta: requisito fundido; níveis lista/standard/full; citations em todos os níveis; relations por ref/summary; adjacência declarada; tectos 52/55 por decisão do lead (a); notas por referência; descrições ≤600 EN com histórico em sbd://toe/version; task opcional em declarativo; bundle pin unchanged KG v1.12.0). Tag on the commit introducing this row. 0.20.0 e 0.19.4 intocadas e alcançáveis. | <sha> | Pedro Farinha (lead) |

## Guardas
Não tocar em 0.20.0 nem em `latest` antes do passo 5; pino v1.12.0 intocado (re-pino v1.27 é ciclo próprio); nada de hashes
inventados — todos os shas vêm do `git log` / da corrida do `release.yml`.
