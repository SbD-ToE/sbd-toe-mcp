---
ai_disclosure:
  tool: "Claude Fable 5.1 (Claude Code)"
  role: "drafting under Pontifex persona, operator-reviewed"
  date: "2026-09-25"
---

# Decisão (a) ligada — tectos 52/55 — e §6-a: os lotes somam o todo (FECHADA)

**Data:** 2026-09-25 · **Persona:** Pontifex · **Para:** Orchestrator
**Ordem:** mensagem directa 2026-09-25 (decisão do lead (a), adenda no despacho da §1 — verificada no hub antes de tocar).
**Ramo:** `0.21` (ver `git log`). **Pino:** KG v1.12.0 — INALTERADO. **0.20.0 e `latest`** — intocados. Sem tag, sem merge.

## 1. Tectos ligados: lista 52 · standard 55 · full sem tecto

Envelopes 8.450/9.200 inalterados. `REQUIREMENT_CEILING_BY_DETAIL` = `PROPOSED_CEILING_BY_DETAIL` por construção
(teste: se a medição mudar e divergirem, há decisão a pedir). `CEILING_FIT.fits=true`; desajuste declarado e
`KNOWN_TOTAL_DEVIATIONS` retirados. Decisão local 0003 emendada (adenda). A fixture 2 do EPIC (69) passa a
bloquear DECLARADO em lista/standard (lotes que somam o todo); o full serve-a. Testes das fixtures adaptados.

## 2. §6-a — a condição da decisão: decomposição que preserva os activadores e soma o todo

- Cada lote = **declaração estrutural** `categories` (partição exacta das categorias activadas, ≤ tecto),
  `technologies`/`changed_files` preservados **literalmente**, `exposure`/`data_sensitivity` preservados pelo
  **efeito** (as categorias que produziram entram na partição; `derived_from` di-lo em cada lote). Re-declará-los
  literalmente somaria as suas categorias a todos os lotes — nenhum caberia (`public` activa 5 concerns).
- Capacidade do lote reserva os extras por regra nomeada das tecnologias (SES-008 por `jwt`).
- Contagem **real** por lote; `union {requirements, recall}` declarada; sugestão ensina a receita por categorias
  (a frase «activadores largos ficam FORA» desapareceu).
- **Forma B no prepare** (`chapters`/`categories`, como no select): necessária para o lote ser executável; a
  declaração estrutural conta como declaração no gate.

**Prova exigida — m_recall da união = 1, lotes EXECUTADOS (não confiamos na contagem):**

| caso | lotes | recall |
|---|---|---|
| 83 real (auth+integrity+deployment/L3) | [IAC,CNT,INT,DPL] 48 · [ACC,AUT,SES,DST] 35 | 83/83 = 1 |
| avaliação (auth+api+validation, public, personal/L3 → 89) | [ARC,ACC,AUT,LOG,API] 52 · [ENC,SES,VAL,ERR,PRI] 37 | 89/89 = 1 |
| avaliação + jwt (90) | capacidade 51, SES-008 reservado | 90/90 = 1 |
| avaliador original (api, public, personal, FastAPI/L3 → 89) — TC-F-34 | [52] · [37] | 89/89 = 1 |

Declarado: o tecto é por-id e a variância por caso é real — o lote de 48 (IAC/CNT/DPL) custa 10.146 tk em lista
(> 8.450), e o payload di-lo (`within_envelope:false`). Não é silêncio; é o limite da promessa por contagem.

## 3. Prova

vitest 804/804 · check · smoke · acceptance 141/16/0/23 gate PASS (TC-F-34 executa os lotes) · Eixo H 10/10 ·
invariante 3 vs oráculo 0.20.0 ✅. Colateral: uma reescrita do TC-F-34 apagou por engano TC-F-35..46 (total
168) — detectado no rollup e reposto do HEAD antes do fecho (total 180).

## 4. Fica

Resto da §6/§7 pela ordem do despacho de 11/09 (notas por referência → `sbd://toe/notes/{id}`; changelog fora
das descrições → `sbd://toe/version`; `next` do contexto; `size_estimate` onde falte; `task` obrigatório vs
contrato; `minimal` residual de select/threat) — à ordem, uma fase por relatório. (c) no Mensor.
