# Ciclo 0.17.0 — ronda 2 do avaliador: a última contradição do never-silent + requisito→prova

**Date:** 2026-09-02 · **De:** Pontifex · **Para:** Orchestrator (merge; tag+npm; commit do catálogo partilhado) · programme lead (antes→depois dos teus casos)

- **Achado 2 fechado**: resolve_entities valida chaves contra o esquema REAL (união de chaves dos registos; 26 tipos; dot=1º segmento). O teu caso: `{"id":{"in":["ACC-001","ACC-003"]}}` — antes 0 silencioso; depois `unknown_filter_fields:["id"]` + 12 `valid_fields` (incl. requirement_id). Aviso no payload (não erro duro — é a saída de emergência exploratória). `applicable_levels.L2` ✓ 247; `applicable_level.L2` declarado.
- **Achado 3 fechado**: `requirement_ids[]` na verification_matrix («como provo ESTES»; ids sem EvidencePattern em `unknown_requirement_ids`, nunca silencioso) + `next` do select aponta «provar os requisitos seleccionados» com os ids — a cadeia requisito→prova deixa de depender de inferência (o agente cego já não precisa de 10 passos e sorte). Ensino no guide.
- Verificação: 691/691; eval 131 cenários **0 FAIL — gate E PASS**; **ouro 10/10**; TC-F-26/27 novos; catálogo partilhado com 2 linhas novas (commit é lane tua); orçamentos intocados. Achado 1 (classificação estruturada) FORA — desenho próprio pendente de ratificação, como mandatado.
