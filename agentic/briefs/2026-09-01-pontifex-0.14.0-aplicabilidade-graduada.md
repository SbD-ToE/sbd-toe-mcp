# Ciclo 0.14.0 — aplicabilidade GRADUADA servida (a noção binária morreu; 4ª instância de conteúdo-em-código fechada)

**Date:** 2026-09-01 · **De:** Pontifex · **Para:** Orchestrator (merge; depois tag+npm) · programme lead/Author (registo da semântica servida)

- **Decisão do Author aplicada verbatim**: presença de capítulo é incondicional; a exigência escala L1→L3, derivada da `proportionality` AUTORADA dos assignments do bundle (obrigatório/recomendado/opcional; texto livre = `specific`, nunca reclassificado), com âncora à matriz canónica do cap. 01 (`addon 05-matriz-controlos-por-risco`).
- **Morreram**: `RISK_LEVEL_CHAPTERS`, `ACTIVE_CHAPTERS_BY_RISK`, `minLevel`, hack "13 apenas L3", razão "Sempre para L2+" do cap. 06. Conjunto de capítulos deriva do bundle_catalog + fallback DECLARADO do `00-fundamentos` (precedente base-set). Módulo único: `src/serving/applicability.ts`.
- **Superfícies**: map (chapters[15] com demand/dominant + semantics + anchor + role_view via projectRole — coerente com guide_by_role), resource chapter-applicability, setup prompt ("No chapter is excluded"), list_chapters (anota, nunca filtra; demand_by_level).
- **Os dados confirmam a semântica**: obrigatórios L1 76 → L2 234; cap. 06 L1 = 10 obrig/3 rec/3 opc; cap. 13 L1 presente com exigência leve. Payload map ≈1.470 tokens (2.325 com role_view).
- **Verificação**: 689/689 (16 testes binários legados re-baselinados); cenários TC-A-06/07/12 + TC-E-10 re-baselinados na MESMA alteração (runner + doc §4.4); eval 121 cenários **0 FAIL, gate E PASS** (E-10 subiu a PASS pleno); **ouro 10/10** (selecção intocada). Bundle pin INALTERADO.
- Fluxo: PR → merge (Orchestrator) → tag `v0.14.0` → npm `latest`.
