---
ai_disclosure:
  tool: "Claude Fable 5.1 (Claude Code)"
  role: "drafting under Pontifex persona, operator-reviewed"
  date: "2026-09-25"
---

# 0.21 §7 — o eixo é um só: FECHADA. E o fecho da linha: tabela final, diferenças de superfície, release preparada

**Data:** 2026-09-25 · **Persona:** Pontifex · **Para:** Orchestrator (→ lead: a release é acto teu)
**Ordem:** mensagem directa 2026-09-25 (após aceitação da §6 @ `27226a9`): §7, última fase; tabela final 27/83 × 3 níveis,
tools/list e lista completa do que muda face à 0.20.0; preparar a release, não executar.
**Ramo:** `0.21` (ver `git log`). **Pino:** KG v1.12.0 — INALTERADO. **0.20.0 e `latest`** — intocados. Sem tag, sem merge.

## 1. §7 — harmonizado, não declarado à parte

O `minimal` do `select` e do `get_threat_landscape` é o mesmo eixo do prepare (inline vs por referência): no select o
`standard` põe as justificações do trace numa legenda e o `minimal` tirava ainda `type`/`source_chapter` (deriváveis); no
threat `standard`/`minimal` referenciam os controlos pela legenda. Passa a **`lista`/`standard`/`full`** nos dois;
`minimal`/`ultrathin` devolvem `-32602` com a indicação (→ lista); no threat **`lista` ≡ `standard`** — declarado no
schema e provado (`detail-axis.test.ts`: as três tools, o mesmo eixo, os mesmos nomes retirados). Descrições dos dois
`detail` em inglês, ≤600.

## 2. A tabela final da linha (tokens ≈ chars/4)

| caso | nível | 0.20.0 | **0.21.0** |
|---|---|---:|---:|
| `auth`/L2, **27** | lista (era minimal, sem descrição) | 3.879 | **4.601** (com descrição, verify, evidence) |
| | standard | 4.403 | **5.082** (+ adjacência detalhada) |
| | full | 11.148 | **7.659** (−31%) |
| `auth+integrity+deployment`/L3, **83** | lista | — | 14.889 → **bloqueia** (tecto 52; lotes 48+35, recall 1) |
| | standard | — | 15.274 → bloqueia (tecto 55) |
| | full | — | **23.781** (sem tecto, preço declarado) |
| fixture 1 (41) | lista / standard / full | ≈18.903 (full) | 6.775 / 7.281 / 12.759 |
| fixture 2 (69) | lista / standard / full | ≈24.731 (full) | bloqueia / bloqueia / 20.549 |
| **tools/list** | | **13.809** | **11.410** (−17,4%) |

Tectos por contagem lista 52 · standard 55 · full sem tecto (decisão (a)); envelopes 8.450/9.200; `within_envelope`
declarado. Achado de CONTRATO com o lead: contagem vs tokens (variância por caso real).

## 3. O que muda na superfície face à 0.20.0

Lista completa e numerada (20 itens) na secção «0.21.0 — RASCUNHO das release notes» do `CHANGELOG.md` — é a peça
para a release e para o Mensor. Em resumo: requisito fundido; três níveis (ultrathin retirado, minimal→lista em prepare,
select e threat); `citations` em todos os níveis (citation_map morreu); relations por ref/summary; adjacência em todos
os níveis; instruções inline; grounding por ref nos dieted; descrição dos controlos directos em todos os níveis;
`size_estimate` em todas as tools; tectos 52/55 com lotes que somam o todo; forma B no prepare; notas por referência;
`task` opcional em declarativo; descrições ≤600 em inglês com histórico em `sbd://toe/version`; `next` do contexto;
recursos `sbd://toe/notes(/{id})`; colaterais (resolve_entities, guia, runner do Eixo H).

## 4. Release — preparada, não executada

`package.json` bumped para **0.21.0** (sem tag). Plano com os actos do lead, por ordem, e a linha sugerida para o
FREEZE-REGISTRY: `agentic/planeado/2026-09-25-release-0.21.0-preparacao.md`. Nada de shas inventados.

## 5. Prova (pós-§7, pós-bump)

vitest verde (69 ficheiros) · `check` · smoke · `eval:acceptance` gate PASS (141/16/0/23) · Eixo H 10/10 · invariante 3 ✅.
