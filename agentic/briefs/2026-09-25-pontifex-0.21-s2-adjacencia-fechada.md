---
ai_disclosure:
  tool: "Claude Fable 5.1 (Claude Code)"
  role: "drafting under Pontifex persona, operator-reviewed"
  date: "2026-09-25"
---

# 0.21 §2 — a adjacência liga-se ao prepare: FECHADA. A tabela final e os tectos-proposta

**Data:** 2026-09-25 · **Persona:** Pontifex · **Para:** Orchestrator (→ lead: §3 desta nota é a decisão)
**Ordem:** mensagem directa do Orchestrator 2026-09-25 (após aceitação da §3 @ `de17d55`): «avança a §2 de forma a
caber POR DESENHO; deriva os tectos com a fórmula sobre o declive real; apresenta como PROPOSTA, não ligues».
**Ramo:** `0.21` (ver `git log`). **Pino:** KG v1.12.0 / `c21d35cb…bece2f` — INALTERADO. **0.20.0 e `latest`** — intocados. Sem tag, sem merge.

## 1. O que aterrou (só §2)

- `adjacency` em **todos os níveis**, a seguir ao `activated_scope`: top-5 por `would_add` + `scanned` +
  `would_change_the_set` (denominador) + `shown`. É o módulo de `a32ca44` sem reinterpretação (teste: bloco
  byte-igual ao do módulo para a mesma declaração). Nunca nomeia um sinal já declarado. Prosa fora do payload
  (legenda `detail_encoding.adjacency` + descrição da tool).
- **Detalhe** (lista completa; o resumo é o prefixo): inline em `standard`/`full`, por **referência executável**
  em `lista` (`detail_ref` → `detail='standard'`). `LEVEL_FORM.adjacency_detail` decide — é o separador
  `lista`↔`standard`, que até aqui só diferiam no eco.
- `discover`: adjacência relativa à declaração feita (não ao conjunto inferido) — declarado.

## 2. A tabela final pedida (27 e 83 × 3 níveis; tokens ≈ chars/4)

| caso | lista | standard | full |
|---|---:|---:|---:|
| **27** (`auth`/L2) — 0.20.0 | 3.879 (minimal, sem descrição) | 4.403 | 11.148 |
| **27** — 0.21 §1+§3+§2 | **5.066** (adjacência 170) | **5.504** (adjacência 608) | **7.813** (−30% vs 0.20.0) |
| **83** (`auth+integrity+deployment`/L3, caso real) — 0.21 | **15.412** (>8.450 — `within_envelope:false`) | **15.755** (>9.200 — idem) | **23.935** (sem tecto, preço declarado) |
| 53 | 8.532 | 8.845 | 15.867 |
| 89 (tecto levantado só para medir) | 13.310 | 13.579 | 16.696 |

Regressão 27→89: **lista** 133,0 tk/req, base **1.476**; **standard** 130,2 tk/req, base **1.987**.

## 3. Os tectos derivados — PROPOSTA (não ligados)

`floor((envelope − base) / custo)` sobre as constantes medidas da forma servida completa:

| nível | envelope herdado | base | custo/req | **tecto proposto** | ligado hoje (ratificado) | custo no ligado |
|---|---:|---:|---:|---:|---:|---:|
| lista | 8.450 | 1.476 | 133,0 | **52** | 83 | ≈12.515 |
| standard | 9.200 | 1.987 | 130,2 | **55** (54 com declive único 133) | 88 | ≈13.445 |

`PROPOSED_CEILING_BY_DETAIL` exporta-os; um teste garante que **não** são os ligados. O que o lead decide:
(a) ligar 52/55 sobre os envelopes herdados — a lista cabe até 52 requisitos **com** descrição, verificação,
prova e adjacência; ou (b) manter 83/88 e subir os envelopes para ≈12.500/13.500; (c) verify/evidence por
referência continua fora — é do Mensor (primeira célula da ablação). Até lá: 83/88 ligados, desajuste declarado.

## 4. Prova

Invariante 3 vs oráculo 0.20.0 ✅ (a adjacência não acrescenta ids) · Eixo H **10/10** · `detail_ref` executado ·
bloco ≡ módulo · vitest **825/825** · `check` · smoke · `eval:acceptance` **141/16/0/23, gate PASS**
(TC-F-47: adjacência 5/38 em lista, detalhe 38 em standard).

## 5. Fica em aberto

Decisão do lead (§3); §6/§7 (changelog fora das descrições → `sbd://toe/version`; notas → `sbd://toe/notes/{id}`;
`next` gerado; `size_estimate` nas restantes tools; gate do `task`; `minimal` residual de `select`/`threat`) —
à ordem. 2-vs-3 níveis: com a §2 há um separador real (detalhe da adjacência, ~440 tk); o `manual_grounding` (§4)
continua por referência em ambos — o Mensor mede.
