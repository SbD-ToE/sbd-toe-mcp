---
ai_disclosure:
  tool: "Claude Fable 5.1 (Claude Code)"
  role: "drafting under Pontifex persona, operator-reviewed"
  date: "2026-09-25"
---

# 0.21 §6 «também nesta vaga»: FECHADA — o que o consumidor paga a cada volta desceu, e nada se perdeu

**Data:** 2026-09-25 · **Persona:** Pontifex · **Para:** Orchestrator
**Ordem:** mensagem directa 2026-09-25 (após aceitação de (a)+§6-a @ `6ea4789`): «o resto da §6/§7 pela ordem do
despacho de 11/09 — notas por referência (c), changelog fora das descrições (d), next gerado do contexto,
size_estimate onde falte, task obrigatório vs contrato, minimal residual em select/threat. Uma fase por relatório.»
**Ramo:** `0.21` (ver `git log`). **Pino:** KG v1.12.0 — INALTERADO. **0.20.0 e `latest`** — intocados. Sem tag, sem merge.
**Este relatório é a §6.** O `minimal` residual de `select`/`threat` (§7) fica para o seguinte.

## 1. O que aterrou

| item | o que mudou | medida |
|---|---|---|
| (c) notas por referência | `src/serving/notes.ts` (11 notas); `sbd://toe/notes` + `/notes/{id}`; payload leva `note_id` + 1 cabeçalho `notes`; nada removido | lista 5.066 → **4.601** tk; standard → 5.082; full → 7.659 |
| (d) changelog fora das descrições | 29 descrições ≤600 chars, inglês, só o que a tool faz; `detail` 1.364→580, `record_type` 606→434; anteriores **verbatim** em `sbd://toe/version.surface_history`; frases de comportamento (fonte única) traduzidas e encurtadas; estatutos pragmáticos nas primeiras palavras | tools/list **13.809 → 11.390 tk** (−17,5%) |
| `next` do contexto | assess: capítulo avaliado (ou do KPI em falta) em vez de `07-cicd-seguro` literal; macro-processes: primeiro MP da ordem publicada + primeiro capítulo que atravessa | 2 literais retirados |
| `size_estimate` em todas as tools | `withDeclaredSize` — régua única servidor + guia; re-mede sobre o payload entregue | 26/29 tools (3 de prosa: n/a declarado) |
| `task` vs contrato | declarativo: task opcional, sem gate (é `recorded_context`); discover: gate como antes; declaração inerte ⇒ `needs_input` com `valid_values` (o erro nomeia o vocabulário) | TC-D-02/03 → discover; célula FALTA da matriz fechada |

## 2. O que a bateria disse pelo caminho (e o que fiz)

- A matriz banda × superfície apanhou uma célula FALTA nova: com o gate do task fora, um `stack` fora do
  vocabulário recebia `needs_clarification` sobre o task. Corrigido no servidor (`needs_input` + `valid_values`),
  não na sonda.
- O guia anunciava tamanhos medidos em processo; com o `size_estimate` do servidor o arredondamento saltou
  (47k→48k). Passou a medir com a mesma régua do servidor.
- O `get_threat_landscape` declarava um preço 53 chars abaixo do entregue (media antes da banda `next`). O
  servidor re-mede sempre.
- Cenários que codificavam frases portuguesas das descrições (TC-F-59, TC-F-71) e o portão de âmbito em modo
  declarativo (TC-D-02/03) foram alinhados com a superfície — a exigência é a mesma, a língua e o modo mudaram.

## 3. Prova

vitest **813/813** (68 ficheiros; novo `surface-descriptions.test.ts`) · `check` · smoke · `eval:acceptance`
**141/16/0/23 gate PASS** · Eixo H **10/10** · invariante 3 ✅ · matriz sem FALTA.

## 4. Fica

- **§7:** `minimal` residual em `select`/`threat` (nome de outro eixo, agora inconsistente com o prepare) — à ordem.
- Achado de CONTRATO já subido (tecto por contagem vs promessa em tokens) — com o lead; até decidir, contagem +
  `within_envelope` declarado.
- 3 tools de prosa sem `size_estimate` (não há onde) — declarado na régua.
