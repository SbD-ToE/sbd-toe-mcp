---
ai_disclosure:
  tool: "Claude Fable 5.1 (Claude Code)"
  role: "drafting under Pontifex persona, operator-reviewed"
  date: "2026-09-25"
---

# 0.21 §1 — o requisito fundido: FECHADA. E um achado sobre os tectos, para o lead

**Data:** 2026-09-25 · **Persona:** Pontifex · **Para:** Orchestrator (+ lead: achado §3)
**Despacho:** `sbd-ai-runtime/handover/em-curso/2026-09-25-orchestrator-pontifex-despacho-s1-fusao.md`
(origem 2026-09-11 forma-da-resposta, mantida). **Ramo:** `0.21`, base `cb36422` (confirmado: não se moveu).
**Pino:** KG v1.12.0 / `c21d35cb…bece2f` — INALTERADO. **0.20.0 e `latest`** — intocados. Sem tag, sem merge.

## 1. O que aterrou (só §1 — uma fase por relatório)

- **Um objecto por requisito** em todos os níveis: `{id, name, type, description, verify, evidence}`,
  verbatim do bundle (1:1 com `evidence_patterns.json`). **A descrição nunca sai.** O bloco
  `g2_context.evidence_patterns` deixou de existir; o que não vai inline (`evidence_pattern_id`,
  `control_id`, `expected_artifact_type_ids`) passa a **referenciado** em
  `completeness_report.verification.by_ref` (matriz, ≤50 ids/chamada); os padrões que só tocavam o
  âmbito por um controlo activado contam-se e referenciam-se (`related_by_control_outside_scope`).
  Denominadores que fecham; `partial` declara os padrões publicados com `verification_logic` vazio (EP-ENC-*).
- **Níveis `lista`/`standard`/`full`.** `ultrathin` retirado do schema, `minimal` → `lista`; ambos
  devolvem `-32602` a dizer para onde foram. Instruções + template **inline** em todos os níveis;
  grounding por referência (`entries_ref` → full) nos dieted; `full` inline e sem tecto, com
  `size_estimate`. Até à §2, `lista` ≡ `standard` fora do eco (declarado; `LEVEL_FORM` é tabela).
- **Tectos ratificados ligados:** lista 83 / standard 88 / full sem tecto; envelopes 8.450 / 9.200.
- **Colaterais de serving** (Pontifex): `resolve_entities` anunciava um filtro válido de
  `evidence_pattern` como desconhecido enquanto o aplicava (esquema derivado da cache v0 inteira) —
  corrigido; o runner do Eixo H lia `requirement_id` — lê `id`; guia/skill/instrução de test-plan
  deixam de ensinar o bloco morto.

## 2. Aceitação (a do despacho, ponto a ponto)

| Critério | Resultado | Evidência |
|---|---|---|
| Ids citáveis idênticos à 0.20.0 (invariante 3) | ✅ 9 casos × 3 níveis | `__snapshots__/citable-ids-0.20.0.json` (gerado antes da §1, de `cb36422`) + `invariant3-golden.test.ts`; Eixo H **10/10** |
| `lista` nos 27 reqs de auth/L2 ≈ 3.900 tk **com** descrição | ❌ **4.918 tk** com descrição+verify+evidence | ver §3 — o número do §5 não se reproduz porque a projecção estava errada, não a implementação |
| Nada deixa de ser declarado; cada ref alcançável | ✅ executadas | `entries_ref`→full, `by_ref`→matriz (`validation_method == verify`), `related…ref`→resolve — `lista.test.ts` |
| Bateria completa | ✅ | vitest 813/813 · check · smoke · `eval:acceptance` 141/16/0/23 gate PASS (`acceptance-reports/2026-09-25-v0.20.0-acceptance.md`) |

## 3. ACHADO — os tectos 83/88 assentam numa projecção defeituosa (decisão do lead)

A projecção do §5 (`s5-form-projection.mjs`, 2026-09-11, minha) fazia o join verify/evidence a partir
de `g2_context.evidence_patterns` do payload full — **um bloco capado a 25**. Só 25 requisitos levavam
verify/evidence na projecção; nos casos maiores o custo por requisito estava subestimado (81 tk/req
medidos vs ~132 reais). Foi essa projecção que derivou 83/88, e é isso que o lead ratificou.

Medição da forma servida (regressão 27→89, os 5 casos do §5; script corrigido, `--served`):

| | lista | standard |
|---|---:|---:|
| declive / base (servida §1) | 133 tk/req / 1.327 | 133 / 1.328 |
| custo projectado NO tecto ratificado | **12.287** (env. 8.450) | **12.946** (env. 9.200) |
| tecto que o envelope herdado suportaria (mesma fórmula) | **53** | **59** |
| projecção **corrigida** da forma-alvo (§1+§3+§2), caso base 27 | 4.341 (não 3.900) | 4.826 |

Cada requisito fundido custa ~88 tk (id+name+type+description ≈ 45; verify+evidence ≈ 43); o resto
do declive são controlos/entidades/citações. **Não re-derivei nada** (despacho: «não se re-derivam
outra vez»). O que fiz foi tornar o desajuste **declarado**, nunca silencioso: `CEILING_FIT` nas
constantes; `size_estimate.within_envelope` (+ nota) em cada payload; `KNOWN_TOTAL_DEVIATIONS` no
`budget.test` (fixture 2: 12.745 vs 8.450). A fixture 1 (41 reqs) cabe (7.104).

**Opções para o lead (não decido):** (a) manter os envelopes e re-derivar os tectos com a medição
real (≈53/59); (b) manter 83/88 e subir os envelopes (≈12.300/13.000); (c) tirar `verify`/`evidence`
da `lista` e deixá-los por referência (viola a promessa escrita do §5 §2 — desaconselho); (d) esperar
pela §3 — que só recupera ~230 tk de base (relations_ref/legendas), não muda o declive.

## 4. Fica em aberto (não fixei)

- 2 ou 3 níveis — `LEVEL_FORM` parametrizável; separador real chega com a §2 (adjacência) / §4 (Mensor).
- §3 (relations saem; citation_map do full inverte-se) e §2 (adjacência ligada ao prepare): fases próprias.
- `select_sbd_toe_requirements` / `get_threat_landscape` mantêm o seu `detail: "minimal"` — é outro eixo
  (serialização de trace/legenda), não lhe toquei; nome inconsistente entre tools, para o Orchestrator.

## 5. Artefactos

Commit no ramo `0.21` (ver `git log`). Medição reprodutível: `npm run build && node scripts/measure/s5-form-projection.mjs`.
Oráculo invariante 3: `src/tools/__snapshots__/citable-ids-0.20.0.json`. Relatórios: `acceptance-reports/2026-09-25-*`,
`docs/acceptance-runs/2026-09-25-axis-h-selection-v0.20.0.md`. CHANGELOG: secção «0.21 (em curso) — §1».
