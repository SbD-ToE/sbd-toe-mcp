---
ai_disclosure:
  tool: "Claude Fable 5.1 (Claude Code)"
  role: "drafting under Pontifex persona, operator-reviewed"
  date: "2026-09-25"
---

# 0.21 §3 — os cortes: FECHADA. A base desce; o declive não — o número novo para o lead

**Data:** 2026-09-25 · **Persona:** Pontifex · **Para:** Orchestrator (+ lead: §3 desta nota)
**Ordem:** mensagem directa do Orchestrator 2026-09-25 (após aceitação da §1 em `28cc817`): «avança a §3 —
relations saem, citation_map inverte, legenda+lista — reporta com a re-medição dos 27 e dos 83 nos três níveis».
**Ramo:** `0.21` (ver `git log`). **Pino:** KG v1.12.0 / `c21d35cb…bece2f` — INALTERADO. **0.20.0 e `latest`** — intocados. Sem tag, sem merge.

## 1. O que aterrou (só §3)

- **`citations` em todos os níveis.** O `full` serve o `citations` invertido (legenda por fonte + ids por
  caminho do payload; no full os caminhos são de lista). `citation_map` deixou de existir. `citableIds()`
  exportado — a regra publicada é a única regra de leitura. Instruções/template/guia/skill/README/descrições
  dizem `citations`; condição de slot `citations_empty`.
- **As relations saem.** `full`: `relations_ref` (chamadas executáveis `trace_sbd_toe_graph` + contabilidade
  exacta); `include_relations=true` repõe as clássicas. `lista`/`standard`: `relations_summary` com a MESMA
  contabilidade; residuais inline quando existem. Achado de dados menor, declarado: no caso L3 de 3 concerns,
  6/151 arestas apontam para slices não activadas (recuperáveis pelo `slice_id` da entidade) e 2 são residuais
  (lacuna do bundle) — ficam inline, nunca em silêncio.

## 2. A re-medição pedida (tokens ≈ chars/4; os 27 e os 83 nos três níveis)

| caso | lista | standard | full |
|---|---:|---:|---:|
| `auth`/L2, 27 reqs — 0.20.0 | 3.879 (minimal) | 4.403 | 11.148 |
| … §1 | 4.918 | 4.919 | 10.297 |
| … **§3** | **4.893** | **4.894** | **7.202** (−35% vs 0.20.0) |
| **83 reqs** (`auth+integrity+deployment`/L3, caso real) — §3 | **15.239** (>8.450, declarado) | 15.239 (>9.200, declarado) | 23.419 (sem tecto) |
| 53 reqs — §3 | 8.290 | 8.290 | 15.313 |
| 89 reqs (tecto levantado só para medir) — §3 | 13.138 | 13.139 | 16.256 |

Regressão 27→89 (5 casos do §5): **declive 133 tk/req, base 1.302** (§1: 1.327). Custo no tecto ratificado:
**≈12.341 (lista/83), ≈13.007 (standard/88)**; tectos que os envelopes herdados suportam pela mesma fórmula:
**53 / 59**. Fixtures do EPIC: f1 full 12.276 (era 18.903), lista 7.066 (cabe); f2 full 20.067, lista 12.695
(fora do envelope — declarado no payload e em `KNOWN_TOTAL_DEVIATIONS`).

## 3. Para o lead — o número novo à frente

A §3 fez o que podia: **−35% no full, −25 tk na base dos dieted**. Não toca no declive, porque o declive é o
requisito fundido (~88 tk/req só o objecto; ~133 com o resto). Confirma-se o que disse na §1: a opção (d)
«esperar pela §3» **não chega**. Recomendação do Orchestrator, que subscrevo: **(a)** re-derivar os tectos
para os honestos (≈53/59) sobre os envelopes herdados, **+ (d)** a §3 já feita; **(c)** verify/evidence por
referência **não se decide sem medir** — vai para o Mensor como primeira célula da ablação. Até decisão:
83/88 ligados com o desajuste declarado (como está).

## 4. Prova

Invariante 3 vs oráculo da 0.20.0 (9 casos × 3 níveis, full lido por caminhos de lista) ✅ · Eixo H **10/10**
(acceptance e `eval:axis-h`) · `relations_ref` do full executado (superset por execução real) · `relations_summary`
≡ contabilidade do `relations_ref` · vitest **815/815** · `check` · smoke · `eval:acceptance` **141/16/0/23, gate PASS**
(`acceptance-reports/2026-09-25-v0.20.0-acceptance.md`; TC-A-01/02 reescritos para `citations`).

## 5. Fica em aberto

§2 (adjacência ligada ao prepare) — fase seguinte, à ordem; 2-vs-3 níveis (`LEVEL_FORM`); decisão do lead
envelope × forma; `minimal` residual em `select`/`threat` — §6/§7, não harmonizado agora (como ordenado).
