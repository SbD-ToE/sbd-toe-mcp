---
ai_disclosure:
  tool: "Claude Opus 5 (Claude Code)"
  role: "drafting under Pontifex persona, operator-reviewed"
  date: "2026-09-10"
---

# Promoção 0.20.0 — §2 implementada, PARAGEM antes da tag: a linha declara-se beta em 6 sítios servidos

**Data:** 2026-09-10 · **Persona:** Pontifex · **Para:** Orchestrator
**Despacho:** `handover/em-curso/2026-09-10-orchestrator-pontifex-despacho-promocao-0.20.0.md`
**Base:** `bd1d3b5` (linha `0.20-beta`), pacote `0.20.0-beta.49`, pino KG `v1.12.0` / `c21d35cb…bece2f`.

## Estado

§2 **implementada e medida**. Bateria completa **verde**. **Tag NÃO cortada**, `latest` **não movido**,
nada publicado. Árvore de trabalho com o delta por commitar.

## Porque parei

Regra §5 do despacho: *«se encontrares outra declaração que a release contradiga (algo que diga
«beta» e passe a estável, ou o inverso), enumera e reporta antes de emendar»*. Encontrei — **nas duas
direcções** — e não são as sete do `v1.18-beta`: é uma **segunda família**, sobre a **linha do
pacote**, que a promoção torna falsa. Cortar a tag primeiro fixaria a falsidade num estado imutável
(protocolo §5) e atrás do próprio portão da §3.

Pela adenda («pára quando o achado muda o que o Orchestrator assumiu ao despachar»): o despacho
assumiu que o problema «beta numa release estável» vivia no sufixo da versão do contrato. Vive
também em 6 declarações servidas de prosa. Muda a base e o dimensionamento.

## Família 1 — identidade do contrato (ratificada, IMPLEMENTADA)

Forma escolhida: o identificador **não se renomeia** (tirar-lhe o `-beta` seria graduá-lo, o
contrário da decisão); a maturidade ganha **campo próprio**, e a do pacote é **derivada da versão**,
nunca escrita à mão.

| # | Ficheiro | Mudança | Tipo |
|---|---|---|---|
| 1 | `src/version-info.ts` | `packageMaturity()` — `stable`/`pre-release`/`undeclared`, derivada de `package.json`; `undeclared` quando a versão não se lê (não se assume nenhuma das duas) | novo, não servido directamente |
| 2 | `src/index.ts` (`sbd://toe/version`) | `serving_contract.identity{id,is,suffix_is_not_status}` + `serving_contract.maturity{contract,package,package_version,not_derivable_from_each_other,why_the_contract_is_beta,what_beta_means_here,what_it_does_not_mean}` | **aditivo**; `version` intocado |
| 3 | `src/serving/activation-vocabulary.ts` (`sbd://toe/activation-vocabulary`) | `contract.identity` + `contract.maturity` (forma curta) + tipo alargado | **aditivo**; `version` intocado |
| 4 | `src/tools/select-requirements.ts` | `provenance.note`: acrescenta «o sufixo é NOME, não estado — a maturidade declara-se em sbd://toe/version → serving_contract.maturity» | payload servido, +19 palavras |
| 5 | `package.json` / `package-lock.json` | `0.20.0-beta.49` → `0.20.0` | versão |

Nada renomeado, nada removido, **nenhuma linha de lógica de selecção tocada**.

Medido ao vivo: `pacote: stable` · `contrato: beta` · `package_version: 0.20.0`.

## Família 2 — a LINHA declarada beta (ENUMERADA, NÃO EMENDADA)

Seis declarações servidas que a promoção torna falsas. Cinco dizem «beta» e passariam a estável; a
sexta é o inverso.

| # | Sítio | Texto servido | Direcção |
|---|---|---|---|
| A | `src/index.ts:336` | `serving_contract.line: "0.20-beta (experiência autorizada pelo programme lead 2026-09-05)"` | beta→estável |
| B | `src/serving/activation-vocabulary.ts:202` | `contract.note`: «Nesta linha (0.20-beta, experiência autorizada pelo lead 2026-09-05)…» | beta→estável |
| C | `src/serving/server-surface.ts:46` | descrição do recurso `sbd://toe/activation-vocabulary`: «DECLARATIVE-FIRST (0.20-beta): …» | beta→estável |
| D | `src/tools/prepare-codegen-context.ts:2695` | `line_note`: «Nesta linha 0.20 (beta) o trace_sbd_toe_graph existe…» | beta→estável |
| E | `assets/agent-guide.md:11` | «## ⛳ START HERE — declarative first (linha 0.20-beta, contrato v1.18-beta)» | beta→estável |
| F | `src/index.ts:343` | `migration`: «… Linha estável inalterada.» | **inverso** — passa a ser esta a linha estável, e ela carrega o contrato beta |

O A fica ao lado do novo `maturity.package: "stable"`, no mesmo bloco: o mesmo objecto diria
«pacote estável» e «linha 0.20-beta».

## Família 3 — referências históricas (inventário, NÃO contraditas)

~20 menções da forma «(0.20.0-beta.NN)» que **nomeiam a versão em que um comportamento entrou**
(ex.: `index.ts:1104` «0.20.0-beta.36 — restringe a avaliação a UM capítulo»). Continuam
verdadeiras depois da promoção: a beta.36 aconteceu. **Não tocadas.** Ficam com a varredura de
descrições, parada para a `0.20.1`.

## Bateria (na árvore com a §2 e o bump)

- suite **820/820** (63 ficheiros) · aceitação **180 · 141 PASS · 16 PART · 0 FAIL · 23 SKIP · gate PASS**
- Eixo H: **byte-idêntico à corrida da beta.49** depois de neutralizar versão e estampa — só o
  `generated_at` difere. Prova de zero deriva na selecção.
- Eixo I: **6 SERVIDO · 0 SERVIDO-MAL · 0 NÃO SERVIDO**
- matriz banda×superfície: 29 tools · **0 FALTA · 0 unprobed**
- determinismo: **28 idênticos + 1 após neutralizar · 0 divergências**
- pacote npm: **242 ficheiros**, 57 superfícies derivadas presentes, sem fugas de caminhos privados
- `npm run check`: pino bate com o substrato (release formal v1.12.0, sha256 verificado)

## Pergunta ao Orchestrator (uma)

A Família 2 entra na `0.20.0` ou fica para a `0.20.1`?

**Recomendação: entra.** Não é dívida de prosa — é falsidade **criada pelo acto da promoção**, na
mesma raiz da §2 (a metade «pacote» da frase que a própria §2 escreve). A `0.20.1` é o veículo da
varredura de descrições, que é trabalho de qualidade; isto é correcção de verdade. E a §3 fecha a
superfície servida a seguir à tag.

Se a resposta for «entra», o delta acrescenta 6 edições de string, sem lógica, e a bateria repete-se.
