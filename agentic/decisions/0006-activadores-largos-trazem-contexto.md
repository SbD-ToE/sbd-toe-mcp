**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino."**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino."**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino."**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.w**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino."**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino."**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino."**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.—**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.·**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.D**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.·**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.·**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.B**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.D**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.§**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.M**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.M**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.>**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.U**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.>**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.>**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.V**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.>**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.>**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ê**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.>**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.>**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.>**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.M**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.§**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.Q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.?**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.X**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.U**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.V**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.Y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.D**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.«**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.»**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.—**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.F**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.M**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.Y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.H**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.É**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.w**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.«**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.»**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.[**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.]**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.—**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.—**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.—**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.B**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.V**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.F**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.B**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.w**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.—**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.—**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.—**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.V**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.F**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.V**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.F**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.V**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.F**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.V**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.F**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.V**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.F**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.V**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.F**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.…**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.D**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.D**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.F**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.M**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.Y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ê**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.Q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.U**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.«**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.»**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.«**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.»**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.à**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.É**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.w**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.M**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ú**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.—**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.B**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.—**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ê**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.§**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.§**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ê**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.′**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.§**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.K**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.S**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.B**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.≠**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.+**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.%**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.+**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.%**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.B**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.+**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.+**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.+**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.w**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.+**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.;**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.k**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.|**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.′**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.′**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.6**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.T**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.â**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ú**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.â**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.é**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.=**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.…**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino._**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.«**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ú**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.+**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.»**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.j**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.O**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.N**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.H**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.8**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.M**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.z**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.§**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.5**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ó**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.õ**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.4**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.§**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.7**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.E**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.H**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.0**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.`**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.y**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ê**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.#**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.9**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.D**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.q**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.M**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.1**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.B**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.g**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.?**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.2**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.K**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.G**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.:**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.h**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.C**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.,**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ç**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ã**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.«**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.x**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.»**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.(**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.«**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.→**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.f**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.í**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.»**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.)**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.m**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.R**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.-**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.«**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.A**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.P**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.I**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.p**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.ú**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.b**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino./**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.L**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.+**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.s**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.»**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.á**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.c**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.u**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.l**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.o**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.***Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.d**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.v**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.r**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.i**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.a**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.n**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.t**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.e**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino. **Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.3**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino..**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.
**Estado:** PROPOSTA — **a promessa vem antes do código.** A implementação avança depois do OK do lead, no mesmo branch. Não há
release, PR de merge nem publicação até o Manual fechar. A versão do MCP sai no ciclo completo Manual → KG (re-pino v1.27 +
v1.28) → ontologia v2.11 → MCP. Todos os números abaixo são do bundle pinado actual (v1.12.0), e **serão re-medidos contra o
bundle novo**. A tabela de §3, em particular, é dado e muda com o re-pino.